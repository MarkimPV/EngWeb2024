extends layout

block content
  .w3-card-4
    header.w3container.w3-purple
      h2 Período Record Added: #{p._id}
    pre= JSON.stringify(p) 
    footer.w3-container.w3-purple
      a(href="/periodos") Return